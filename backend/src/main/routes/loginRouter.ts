import { Router } from 'express';
import { adaptRoute } from '@/main/adapters/expressRouteAdapter';
// import { factory1, factory2 } from '@/main/factories/controllers';

export default (router: Router): void => {
 // router.post('/login', adaptRoute(factory1()));
 // router.get('/createUser', adaptRoute(factory2()));
};
